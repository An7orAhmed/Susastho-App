import 'package:flutter/material.dart';

class VaccineAdd extends StatelessWidget {
  const VaccineAdd({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
