import 'package:flutter/material.dart';

class PublishNews extends StatelessWidget {
  const PublishNews({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
